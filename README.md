# FT_LINUX

## Build and Run our LFS (Linux From Scratch)

Create a VM with an ISO ubuntu or kubuntu, etc.

Add a second volume.

To setup disk run :

```sh
./disk_create_and_format.sh
```

Install package

```sh
./install.sh
```
